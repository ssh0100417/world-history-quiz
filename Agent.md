✅ 행동강령 점검 완료 (2026-04-07 기준)

# Agent.md

이 파일은 사용자가 요청한 파일명 호환용 문서다.

세계사 문제 출제 프로그램 관련 실제 작업 지침은 같은 폴더의 `AGENTS.md`에 정리되어 있다.

작업자는 반드시 `AGENTS.md`를 먼저 읽고 따른다.

적용 경로:

```text
C:\Users\pc\Documents\Codex\2026-06-01\codex-react\world-history-quiz
```

핵심 원칙:

- 세계사 문제 출제 프로그램 작업은 위 경로에서만 진행한다.
- 새 프로젝트나 새 repository를 만들지 않는다.
- 기존 단일 React 앱 구조와 `src/App.tsx`를 기준으로 수정한다.
- V3/V4 병합, V4 제거, Netlify 서비스워커 캐시, 1초 렌더링 방지, Git 커밋 대상 파일 규칙은 `AGENTS.md`의 내용을 따른다.
